# Vacío = el frontend llama al mismo host (nginx en Docker o proxy de Vite en dev).
VITE_BACKEND_URL=
# Solo para `npm run dev` sin Docker: puerto del backend local.
VITE_DEV_BACKEND_URL=http://localhost:8081
VITE_HOURS_CLOSE_TICKETS_AUTO=
